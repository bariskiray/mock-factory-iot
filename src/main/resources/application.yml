server:
  port: 8080

spring:
  application:
    name: mock-factory

# ── Mock Factory defaults ──────────────────────────────────────
mockfactory:
  scheduler:
    core-pool-size: 16          # max concurrent device simulations
